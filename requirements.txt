numpy>=1.24
scipy>=1.10
# sympy 1.12 works UNLESS the optional gmpy2 accelerator is installed (as in
# Anaconda): sympy 1.12 + gmpy2 hangs on symbolic/check_impulsive_point_mass.py.
# See the "Known pitfall" note in README.md.
sympy>=1.13
mpmath>=1.3
matplotlib>=3.7
